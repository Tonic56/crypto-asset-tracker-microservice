# Crypto Asset Tracker Microservice

Приложение для отслеживания криптовалютного портфеля в реальном времени, построенное на микросервисной архитектуре с использованием Go.

## 📋 Описание

**Crypto Asset Tracker** — это масштабируемое приложение на основе микросервисов для мониторинга криптовалютных портфелей в режиме реального времени. Пользователи могут регистрироваться, управлять своим крипто-портфелем и получать живые обновления стоимости активов через WebSocket-соединения.

### Ключевые возможности

- 🔐 **Безопасная аутентификация** — JWT-токены с refresh механизмом
- 💼 **Управление портфелем** — добавление, обновление и удаление криптовалют
- 📊 **Real-time обновления** — мгновенные обновления цен через WebSocket
- 🔄 **Событийная архитектура** — асинхронная обработка данных через Redis Pub/Sub и Kafka
- 📈 **Аналитика** — исторические данные в ClickHouse для анализа
- 🚀 **Микросервисная архитектура** — независимые, легко масштабируемые сервисы

---

## 🏗️ Архитектура

### Дизайн системы

Система реализует паттерн распределенных микросервисов со следующими характеристиками:

- **Backend-for-Frontend (BFF)** — Profile Service как единая точка входа для клиентов
- **Независимость сервисов** — каждый микросервис отвечает за свою предметную область
- **Real-time коммуникация** — WebSocket для мгновенной доставки обновлений
- **Событийная архитектура** — Redis Pub/Sub для real-time уведомлений и Kafka для надежной доставки данных в аналитический конвейер.
- **Полиглот персистентность** — PostgreSQL, Redis, ClickHouse для разных задач

### Схема взаимодействия

```
┌─────────────┐
│   Клиент    │
└──────┬──────┘
       │ HTTP/WebSocket
       ▼
┌─────────────────────────────────────────────────────────────┐
│                    Profile Service (BFF)                    │
│  • REST API (Gin)                                           │
│  • WebSocket Manager                                        │
│  • JWT Middleware                                           │
└────┬────────────────┬─────────────────┬─────────────────────┘
     │ gRPC           │ HTTP            │ Redis Pub/Sub
     ▼                ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│Authorization │  │  Aggregator  │  │    Redis     │
│   Service    │  │   Service    │  │   (Pub/Sub)  │
└──────────────┘  └──────┬───────┘  └──────────────┘
                         │ gRPC Stream
                         ▼
                  ┌──────────────┐
                  │    Socket    │
                  │   Service    │
                  └──────┬───────┘
                         │ WebSocket
                         ▼
                  ┌──────────────┐
                  │   Binance    │
                  │   API        │
                  └──────────────┘

      Аналитический конвейер:
      Aggregator → Kafka → Kafka-ClickHouse → ClickHouse
```

### Потоки данных

#### 1. Аутентификация

1. Пользователь отправляет `POST /api/v1/auth/register` или `/login` в Profile Service
2. Profile делегирует запрос в Authorization Service через gRPC
3. Authorization проверяет данные в `postgres-auth`
4. Генерируются JWT-токены (access + refresh)
5. Refresh-токен сохраняется в HTTP-only cookie

#### 2. Управление портфелем

1. Аутентифицированный пользователь отправляет `POST /api/v1/profile/coins`
2. Profile Service сохраняет данные в `postgres-profile`
3. Profile уведомляет Aggregator через HTTP `GET /coin?symbol=btcusdt&id=user-id`
4. Aggregator запускает подписку на символ

#### 3. Подписка на обновления цен

1. Клиент устанавливает WebSocket соединение: `ws://localhost:8080/api/v1/ws`
2. ConnectionManager регистрирует клиента и подписывается на Redis каналы
3. Если это первый подписчик на символ, Aggregator запрашивает gRPC-стрим у Socket Service
4. Socket поддерживает постоянное WebSocket-соединение с Binance API

#### 4. Обработка и доставка данных

1. Socket получает данные от Binance и передает через gRPC-стрим в Aggregator
2. Aggregator обрабатывает сырые данные:
   - **AggTrade** → вычисляет ежесекундные обновления цен (`SecondStat`)
   - **MiniTicker** → подготавливает данные для Kafka (`KafkaMsg`)
3. `SecondStat` публикуется в Redis Pub/Sub (канал = символ монеты)
4. Profile's ConnectionManager получает обновление из Redis
5. Пересчитывает стоимость портфеля: `количество × новая цена`
6. Отправляет обновленный портфель клиенту через WebSocket

#### 5. Аналитический конвейер (опционально)

1. Aggregator отправляет `KafkaMsg` в топик Kafka `binance.miniticker`
2. Kafka-ClickHouse Service читает сообщения батчами
3. Выполняет пакетные вставки в ClickHouse таблицу `crypto.market_tickers`
4. Данные доступны для анализа через Tabix или ClickHouse-Dashboard

---

## 🔧 Микросервисы

### Profile Service (BFF)

**Технологии**: Go, Gin, GORM, Gorilla WebSocket, Redis, gRPC

**Порт**: `8080` (HTTP/WebSocket)

**Обязанности**:
- Главный API-шлюз для клиентских запросов
- Обработка REST API для аутентификации и управления портфелем
- Делегирование аутентификации в Authorization Service (gRPC)
- Хранение профилей пользователей в `postgres-profile`
- Управление активными WebSocket-соединениями
- Подписка на Redis Pub/Sub для получения ценовых обновлений
- Рассчет и отправка обновленного портфеля клиентам

**Ключевые компоненты**:
- `handler.go` — HTTP-обработчики и WebSocket upgrade
- `manager.go` — управление WebSocket клиентами и подписками
- `middleware.go` — JWT аутентификация

**База данных**: `postgres-profile` (профили пользователей, портфели)

---

### Authorization Service

**Технологии**: Go, gRPC, GORM, JWT

**Порт**: `50051` (gRPC)

**Обязанности**:
- Централизованная аутентификация и авторизация
- Регистрация пользователей с безопасным хешированием паролей
- Выпуск и валидация JWT-токенов
- Управление refresh-токенами
- Логаут и инвалидация токенов

**gRPC методы**:
- `Register(RegisterRequest) → RegisterResponse`
- `Login(LoginRequest) → LoginResponse`
- `RefreshToken(RefreshTokenRequest) → RefreshTokenResponse`
- `Logout(LogoutRequest) → LogoutResponse`

**База данных**: `postgres-auth` (учетные данные пользователей, refresh-токены)

---

### Aggregator Service

**Технологии**: Go, Redis (Pub/Sub), Kafka, gRPC (клиент)

**Порт**: `8088` (HTTP управление)

**Обязанности**:
- Получение сырых рыночных данных от Socket Service через gRPC-стримы
- Обработка различных типов данных:
  - **AggTrade** — агрегированные сделки для ежесекундных обновлений
  - **MiniTicker** — мини-тикеры для статистики за 24 часа
- Вычисление `SecondStat` (ежесекундные цены)
- Публикация обновлений в Redis Pub/Sub (канал = символ монеты)
- Отправка `KafkaMsg` в Kafka топик для аналитики
- Динамическое управление подписками на символы

**HTTP эндпоинты**:
- `GET /coin?symbol=btcusdt&id=user-id` — добавить подписку
- `DELETE /coin?symbol=btcusdt&id=user-id` — удалить подписку

**Ключевые компоненты**:
- `stream_manager.go` — управление gRPC-стримами и подписчиками
- `converting/` — конвертация сырых данных в структурированные форматы

---

### Socket Service

**Технологии**: Go, Gorilla WebSocket, gRPC

**Порт**: `50052` (gRPC)

**Обязанности**:
- Отказоустойчивый прокси к внешним WebSocket API (Binance)
- Поддержание стабильных соединений с автоматическим переподключением
- Экспоненциальный backoff при ошибках подключения
- Обработка Ping/Pong для keep-alive
- Предоставление gRPC-стримов для внутренних сервисов

**gRPC методы**:
- `ReceiveRawAggTrade(RawAggTradeRequest) → stream RawResponse`
- `ReceiveRawMiniTicker(RawMiniTickerRequest) → stream RawResponse`

**Ключевые компоненты**:
- `conn.go` — управление WebSocket соединениями с логикой переподключения
- Поддержка нескольких одновременных подписок

---

### Kafka-ClickHouse Service

**Технологии**: Go, segmentio/kafka-go, clickhouse-go

**Обязанности**:
- ETL-сервис для переноса данных из Kafka в ClickHouse
- Чтение сообщений из топика `binance.miniticker`
- Батчирование записей (по размеру или времени)
- Эффективные пакетные вставки в ClickHouse
- Создание и управление таблицами

**Конфигурация батчей**:
- Размер батча: настраиваемый (например, 100 записей)
- Таймаут батча: настраиваемый (например, 10 секунд)

**ClickHouse таблица**: `crypto.market_tickers`

**Ключевые компоненты**:
- `repo.go` — батчированная вставка данных

---

### ClickHouse-Dashboard

**Технологии**: Go, net/http, HTML/CSS

**Порт**: `8083` (HTTP)

**Обязанности**:
- Простой веб-интерфейс для визуализации данных ClickHouse
- Отображение последних записей из `market_tickers`
- Базовая фильтрация по символам

---

## 💾 Хранение данных и обмен сообщениями

### PostgreSQL

Две изолированные базы данных для разделения ответственности:

| База данных | Порт | Назначение | Используется |
|-------------|------|------------|--------------|
| `postgres-auth` | 5432 | Учетные данные пользователей | Authorization Service |
| `postgres-profile` | 5434 | Профили и портфели | Profile Service |

**Примечание**: В таблице указаны порты, доступные на вашем `localhost`. Внутри Docker-сети сервисы обращаются к базам данных по их стандартным портам (например, `5432` для PostgreSQL). Это объясняет, почему в переменных окружения (`.env`) для сервисов указан порт `5432`.

### Redis

**Порт**: `6379`

**Роль**: Высокоскоростная шина сообщений в памяти

**Использование**:
- **Pub/Sub** для трансляции ценовых обновлений в реальном времени
- Каналы именуются по символу монеты (например, `btcusdt`, `ethusdt`)
- Aggregator публикует, Profile подписывается

**Конфигурация**:
- `maxmemory`: 256MB
- `maxmemory-policy`: allkeys-lru

### Kafka + Zookeeper

**Порты**: 
- Kafka: `9092`
- Zookeeper: `2181`

**Роль**: Персистентная, масштабируемая очередь сообщений для аналитического конвейера

**Топики**:
- `binance.miniticker` — рыночные данные с дневной статистикой
  - Партиции: 3
  - Репликация: 1

### ClickHouse

**Порты**:
- HTTP: `8123`
- Native: `9000`

**Роль**: Колоночная СУБД для OLAP-запросов

**Схема данных**:
```sql
CREATE TABLE crypto.market_tickers (
    message_id String,
    event_type String,
    event_time DateTime64(3),
    receive_time DateTime64(3),
    symbol String,
    close_price Decimal64(8),
    open_price Decimal64(8),
    high_price Decimal64(8),
    low_price Decimal64(8),
    change_price Decimal64(8),
    change_percent Decimal64(8)
) ENGINE = MergeTree()
ORDER BY (symbol, event_time)
PARTITION BY toYYYYMM(event_time);
```

---

## 🚀 Начало работы

### Предварительные требования

- **Docker** 20.10+
- **Docker Compose** 2.0+

### Установка и запуск

1. **Клонируйте репозиторий**

```bash
git clone https://github.com/yourusername/crypto-asset-tracker-microservice.git
cd crypto-asset-tracker-microservice
```

2. **Создайте файлы `.env` для каждого сервиса**

Скопируйте `.env.example` в `.env` в директориях:
- `Profile/.env`
- `Authorization/.env`
- `Aggregator/.env`
- `Socket/.env`
- `Kafka-ClickHouse/.env`

Или используйте значения по умолчанию из `compose.yaml`.

3. **Запустите все сервисы**

```bash
docker-compose up --build
```

Или в фоновом режиме:

```bash
docker-compose up --build -d
```

4. **Дождитесь инициализации**

Первый запуск может занять несколько минут:
- Скачивание Docker образов
- Сборка сервисов
- Инициализация баз данных
- Создание Kafka топиков

5. **Проверьте статус сервисов**

```bash
docker-compose ps
```

Все сервисы должны быть в статусе `Up` или `Up (healthy)`.

### Остановка сервисов

```bash
docker-compose down
```

Удаление данных (volumes):

```bash
docker-compose down -v
```

---

## 📖 Руководство пользователя

### API Endpoints

**Base URL**: `http://localhost:8080`

### 1. Регистрация

Создайте новый аккаунт.

**Endpoint**: `POST /api/v1/auth/register`

**Request Body**:
```json
{
  "name": "myuser",
  "password": "mypassword123"
}
```

**Response**: `201 Created`
```json
{
  "message": "user created successfully"
}
```

**Пример с curl**:
```bash
curl -X POST http://localhost:8080/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"name":"myuser","password":"mypassword123"}'
```

---

### 2. Вход в систему

Получите JWT-токены для аутентификации.

**Endpoint**: `POST /api/v1/auth/login`

**Request Body**:
```json
{
  "name": "myuser",
  "password": "mypassword123"
}
```

**Response**: `200 OK`
```json
{
  "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

**Cookies**:
- `refreshToken` — HTTP-only cookie для обновления токенов

**⚠️ Важно**: Сохраните `accessToken` — он потребуется для всех защищенных эндпоинтов.

**Пример с curl**:
```bash
curl -X POST http://localhost:8080/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"name":"myuser","password":"mypassword123"}' \
  -c cookies.txt
```

---

### 3. Получение профиля

Получите информацию о вашем профиле и портфеле.

**Endpoint**: `GET /api/v1/profile`

**Headers**:
```
Authorization: Bearer <accessToken>
```

**Response**: `200 OK`
```json
{
  "ID": "a1b2c3d4-e5f6-7890-1234-567890abcdef",
  "Name": "myuser",
  "Coins": [
    {
      "ID": 1,
      "CreatedAt": "2026-1-27T10:00:00Z",
      "UpdatedAt": "2026-1-27T10:00:00Z",
      "DeletedAt": null,
      "Symbol": "btcusdt",
      "Quantity": "1.5",
      "UserID": "a1b2c3d4-e5f6-7890-1234-567890abcdef"
    }
  ]
}
```

**Пример с curl**:
```bash
curl -X GET http://localhost:8080/api/v1/profile \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

---

### 4. Создание профиля

Создайте профиль пользователя (обычно создается автоматически при регистрации).

**Endpoint**: `POST /api/v1/profile`

**Headers**:
```
Authorization: Bearer <accessToken>
```

**Response**: `201 Created`
```json
{
  "message": "profile created successfully"
}
```

---

### 5. Добавление/Обновление монет в портфеле

Добавьте монету или обновите количество существующей.

**Endpoint**: `POST /api/v1/profile/coins`

**Headers**:
```
Authorization: Bearer <accessToken>
```

**Request Body**:
```json
{
  "symbol": "btcusdt",
  "quantity": "1.5"
}
```

**Поддерживаемые операции**:
- Положительное `quantity` — добавить к текущему количеству
- Отрицательное `quantity` — уменьшить количество

**Response**: `200 OK`
```json
{
  "ID": 1,
  "CreatedAt": "2026-01-27T10:00:00Z",
  "UpdatedAt": "2026-01-27T10:00:00Z",
  "DeletedAt": null,
  "UserID": "a1b2c3d4-e5f6-7890-1234-567890abcdef",
  "Symbol": "btcusdt",
  "Quantity": "1.5"
}
```

**Примеры**:

Добавить BTC:
```bash
curl -X POST http://localhost:8080/api/v1/profile/coins \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"symbol":"btcusdt","quantity":"1.5"}'
```

Добавить ETH:
```bash
curl -X POST http://localhost:8080/api/v1/profile/coins \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"symbol":"ethusdt","quantity":"10.0"}'
```

Уменьшить количество:
```bash
curl -X POST http://localhost:8080/api/v1/profile/coins \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"symbol":"btcusdt","quantity":"-0.5"}'
```

---

### 6. Удаление монеты

Полностью удалите монету из портфеля.

**Endpoint**: `DELETE /api/v1/profile/coins`

**Headers**:
```
Authorization: Bearer <accessToken>
```

**Request Body**:
```json
{
  "symbol": "btcusdt"
}
```

**Response**: `200 OK`
```json
{
  "message": "coin successfully deleted"
}
```

**Пример с curl**:
```bash
curl -X DELETE http://localhost:8080/api/v1/profile/coins \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"symbol":"btcusdt"}'
```

---

### 7. WebSocket — Real-time обновления портфеля

Подключитесь к WebSocket для получения живых обновлений стоимости портфеля.

**Endpoint**: `ws://localhost:8080/api/v1/ws`

**Headers**:
```
Authorization: Bearer <accessToken>
```

**Протокол**: WebSocket

#### Формат получаемых сообщений

После подключения вы будете получать JSON-сообщения при каждом изменении цены:

```json
{
  "UserID": "uuid-here",
  "UserName": "myuser",
  "TotalValue": "136752.975",
  "Coins": [
    {
      "Symbol": "btcusdt",
      "Quantity": "1.5",
      "Price": "68123.45",
      "Total": "102185.175"
    },
    {
      "Symbol": "ethusdt",
      "Quantity": "10.0",
      "Price": "3456.78",
      "Total": "34567.80"
    }
  ]
}
```

**Поля**:
- `UserID` — уникальный идентификатор пользователя
- `UserName` — имя пользователя
- `TotalValue` — общая стоимость портфеля в USDT
- `Coins` — массив монет с текущими данными
  - `Symbol` — символ монеты
  - `Quantity` — количество монет
  - `Price` — текущая цена (обновляется каждую секунду)
  - `Total` — стоимость позиции (Quantity × Price)

#### Пример подключения с wscat

```bash
wscat -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -c ws://localhost:8080/api/v1/ws
```

#### Пример на JavaScript

```javascript
const accessToken = "YOUR_ACCESS_TOKEN";

const ws = new WebSocket('ws://localhost:8080/api/v1/ws', {
  headers: {
    'Authorization': `Bearer ${accessToken}`
  }
});

ws.onopen = () => {
  console.log('✅ WebSocket подключен');
};

ws.onmessage = (event) => {
  const portfolio = JSON.parse(event.data);
  console.log('📊 Обновление портфеля:', portfolio);
  
  // Обновите UI
  updatePortfolioUI(portfolio);
};

ws.onerror = (error) => {
  console.error('❌ WebSocket ошибка:', error);
};

ws.onclose = () => {
  console.log('🔌 WebSocket отключен');
};
```

#### Пример на Python (websockets)

```python
import asyncio
import websockets
import json

async def connect_websocket():
    access_token = "YOUR_ACCESS_TOKEN"
    
    async with websockets.connect(
        'ws://localhost:8080/api/v1/ws',
        extra_headers={'Authorization': f'Bearer {access_token}'}
    ) as websocket:
        print('✅ WebSocket подключен')
        
        async for message in websocket:
            portfolio = json.loads(message)
            print('📊 Обновление портфеля:', portfolio)

asyncio.run(connect_websocket())
```

---

## 📊 Мониторинг и панели управления

После запуска системы доступны следующие веб-интерфейсы:

### Redis Commander

**URL**: http://localhost:8081

**Учетные данные**: `admin` / `admin`

**Возможности**:
- Просмотр активных Pub/Sub каналов
- Мониторинг данных в Redis
- Анализ используемой памяти
- Управление ключами

**Что проверять**:
- Каналы появляются после добавления монет (например, `btcusdt`, `ethusdt`)
- Сообщения публикуются в каналы при обновлении цен

---

### Tabix (ClickHouse UI)

**URL**: http://localhost:8082

**Назначение**: Веб-интерфейс для работы с ClickHouse

**Возможности**:
- Выполнение SQL-запросов
- Просмотр структуры таблиц
- Экспорт данных в различных форматах

**Примеры SQL-запросов**:

```sql
-- Последние 100 записей
SELECT * 
FROM crypto.market_tickers 
ORDER BY event_time DESC 
LIMIT 100;

-- Средняя цена за последний час
SELECT 
    symbol,
    avg(close_price) as avg_price,
    min(close_price) as min_price,
    max(close_price) as max_price
FROM crypto.market_tickers
WHERE event_time >= now() - INTERVAL 1 HOUR
GROUP BY symbol;

-- Статистика по конкретному символу
SELECT 
    symbol,
    count(*) as records,
    min(close_price) as min_price,
    max(close_price) as max_price,
    avg(close_price) as avg_price
FROM crypto.market_tickers
WHERE symbol = 'BTCUSDT'
GROUP BY symbol;

-- Ценовая динамика по часам
SELECT 
    toStartOfHour(event_time) as hour,
    symbol,
    avg(close_price) as avg_price
FROM crypto.market_tickers
WHERE event_time >= now() - INTERVAL 24 HOUR
GROUP BY hour, symbol
ORDER BY hour DESC, symbol;
```

---

### ClickHouse Dashboard

**URL**: http://localhost:8083

**Назначение**: Простая кастомная панель для визуализации данных

**Возможности**:
- Отображение последних записей из `market_tickers`
- Базовая фильтрация по символам
- Визуализация временных рядов

---

### Kafka UI

**URL**: http://localhost:8084

**Назначение**: Управление и мониторинг Apache Kafka

**Возможности**:
- Просмотр топиков (например, `binance.miniticker`)
- Мониторинг сообщений в топиках
- Отслеживание состояния консьюмеров
- Управление конфигурацией Kafka

**Основные разделы**:
- **Topics** — список всех топиков, количество партиций
- **Consumers** — активные группы консьюмеров и их лаги
- **Brokers** — состояние брокеров Kafka

---

## 🔐 Безопасность

### Аутентификация и авторизация

- **JWT-токены** для stateless аутентификации
- **Access Token** — короткий срок жизни (например, 15 минут)
- **Refresh Token** — длительный срок жизни (например, 30 дней)
- HTTP-only cookies для refresh-токенов (защита от XSS)

### Хеширование паролей

- Использование стандартных алгоритмов хеширования (bcrypt)
- Соль генерируется автоматически для каждого пароля

### Изоляция данных

- Каждый сервис имеет свою изолированную базу данных
- PostgreSQL: `postgres-auth` и `postgres-profile` полностью разделены

### Межсервисная коммуникация

- gRPC для безопасной коммуникации между микросервисами
- Внутренняя сеть Docker (`crypto-network`)

---

## 📈 Масштабируемость

Микросервисная архитектура обеспечивает:

### Горизонтальное масштабирование

- Каждый сервис может масштабироваться независимо
- Load balancing между репликами сервисов
- Stateless дизайн Profile Service для легкого масштабирования

### Независимое развертывание

- Обновление одного сервиса не требует перезапуска других
- Blue-green deployments
- Canary releases

### Выделение ресурсов

- Docker Compose позволяет задать лимиты CPU и памяти
- Kafka и ClickHouse могут работать на отдельных серверах

### Изоляция отказов

- Circuit breaker паттерн для gRPC вызовов
- Graceful degradation при недоступности отдельных сервисов
- Автоматическое переподключение в Socket Service

---

## 🛠️ Стек технологий

### Языки и фреймворки

- **Go 1.21+** — основной язык разработки
- **Gin** — HTTP фреймворк для REST API
- **gRPC** — межсервисная коммуникация
- **Protocol Buffers** — сериализация данных

### Базы данных

- **PostgreSQL 15** — реляционная СУБД для пользователей и профилей
- **Redis 7** — in-memory хранилище, Pub/Sub для real-time
- **ClickHouse** — колоночная СУБД для аналитики

### Брокеры сообщений

- **Apache Kafka 2.7.0** — распределенная очередь сообщений
- **Apache Zookeeper** — координация Kafka

### WebSocket

- **Gorilla WebSocket** — библиотека для WebSocket на Go
- Binance WebSocket API — источник рыночных данных

### DevOps

- **Docker** — контейнеризация сервисов
- **Docker Compose** — оркестрация контейнеров

### Библиотеки

- **GORM** — ORM для работы с PostgreSQL
- **JWT-Go** — работа с JWT-токенами
- **segmentio/kafka-go** — Kafka клиент
- **clickhouse-go** — ClickHouse клиент
- **shopspring/decimal** — точные вычисления с дробными числами

---

## 🔍 Дополнительная информация

### Порты сервисов

| Сервис | Порт | Протокол | Назначение |
|--------|------|----------|------------|
| Profile Service | 8080 | HTTP/WebSocket | REST API, WebSocket |
| Authorization Service | 50051 | gRPC | Аутентификация |
| Aggregator Service | 8088 | HTTP | Управление подписками |
| Socket Service | 50052 | gRPC | gRPC стримы |
| PostgreSQL (auth) | 5432 | PostgreSQL | БД авторизации |
| PostgreSQL (profile) | 5434 | PostgreSQL | БД профилей |
| Redis | 6379 | Redis | Pub/Sub |
| Kafka | 9092 | Kafka | Брокер сообщений |
| Zookeeper | 2181 | Zookeeper | Координация Kafka |
| ClickHouse (HTTP) | 8123 | HTTP | HTTP API |
| ClickHouse (Native) | 9000 | Native | Нативный протокол |
| Redis Commander | 8081 | HTTP | UI для Redis |
| Tabix | 8082 | HTTP | UI для ClickHouse |
| ClickHouse Dashboard | 8083 | HTTP | Кастомная панель |
| Kafka UI | 8084 | HTTP | UI для Kafka |

### Переменные окружения

#### Profile Service

```env
ENV=local
HTTP_PORT=8080
GRPC_PORT=50052
POSTGRES_HOST=postgres-profile
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=profile_db
JWT_SECRET=your-super-secret-key-that-is-long
REDIS_ADDR=redis:6379
AUTH_SERVICE_ADDR=authorization-service:50051
```

#### Authorization Service

```env
ENV=local
GRPC_PORT=50051
POSTGRES_HOST=postgres-auth
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=auth_db
JWT_SECRET=your-super-secret-key-that-is-long
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=168h
```

#### Aggregator Service

```env
BROKERS=kafka:9092
REDIS_ADDR=redis:6379
SOCKET_SERVICE_ADDR=socket-service:50051
SERVER_ADDR=:8088
```

#### Socket Service

```env
ADDRESS=0.0.0.0:50051
PORT=:50051
```

#### Kafka-ClickHouse Service

```env
CLICKHOUSE_ADDR=clickhouse:9000
CLICKHOUSE_PASSWORD=postgres
CLICKHOUSE_DATABASE=crypto
CLICKHOUSE_TABLE=market_tickers
KAFKA_BROKERS=kafka:9092
KAFKA_TOPIC=binance.miniticker
KAFKA_GROUP_ID=clickhouse-consumer-group
BATCH_SIZE=100
BATCH_TIMEOUT=10s
```

### Полезные команды Docker

```bash
# Просмотр логов всех сервисов
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f profile-service
docker-compose logs -f aggregator-service

# Перезапуск конкретного сервиса
docker-compose restart profile-service

# Проверка статуса всех сервисов
docker-compose ps

# Остановка всех сервисов
docker-compose down

# Остановка с удалением volumes (УДАЛИТ ВСЕ ДАННЫЕ!)
docker-compose down -v

# Пересборка конкретного сервиса
docker-compose up --build profile-service

# Выполнение команды внутри контейнера
docker-compose exec profile-service sh

# Просмотр использования ресурсов
docker stats
```

### Проверка здоровья сервисов

```bash
# PostgreSQL (auth)
docker-compose exec postgres-auth pg_isready -U postgres

# PostgreSQL (profile)
docker-compose exec postgres-profile pg_isready -U postgres

# Redis
docker-compose exec redis redis-cli ping

# Kafka
docker-compose exec kafka kafka-broker-api-versions.sh --bootstrap-server localhost:9092

# ClickHouse
docker-compose exec clickhouse clickhouse-client --query "SELECT 1"
```

---

## 🐛 Устранение неполадок

### WebSocket не подключается

**Симптомы**: Ошибка при попытке подключения к WebSocket

**Возможные причины и решения**:

1. **Невалидный access token**
   ```bash
   # Получите новый токен через /api/v1/auth/login
   curl -X POST http://localhost:8080/api/v1/auth/login \
     -H "Content-Type: application/json" \
     -d '{"name":"myuser","password":"mypassword123"}'
   ```

2. **Нет монет в портфеле**
   ```bash
   # Добавьте хотя бы одну монету
   curl -X POST http://localhost:8080/api/v1/profile/coins \
     -H "Authorization: Bearer YOUR_TOKEN" \
     -H "Content-Type: application/json" \
     -d '{"symbol":"btcusdt","quantity":"1.0"}'
   ```

3. **Profile Service недоступен**
   ```bash
   # Проверьте логи
   docker-compose logs -f profile-service
   ```

---

### Не приходят обновления цен

**Симптомы**: WebSocket подключен, но нет сообщений

**Возможные причины и решения**:

1. **Socket Service не подключен к Binance**
   ```bash
   # Проверьте логи Socket Service
   docker-compose logs -f socket-service
   # Ищите сообщения о подключении к Binance
   ```

2. **Redis Pub/Sub не работает**
   ```bash
   # Проверьте Redis Commander (http://localhost:8081)
   # Должны быть видны каналы с именами монет (btcusdt, ethusdt)
   ```

3. **Aggregator не получает данные**
   ```bash
   # Проверьте логи Aggregator
   docker-compose logs -f aggregator-service
   ```

---

### Ошибки аутентификации

**Симптомы**: `401 Unauthorized` или `403 Forbidden`

**Возможные причины и решения**:

1. **Authorization Service недоступен**
   ```bash
   # Проверьте статус
   docker-compose ps authorization-service
   
   # Проверьте логи
   docker-compose logs -f authorization-service
   ```

2. **PostgreSQL (auth) недоступна**
   ```bash
   # Проверьте соединение
   docker-compose exec postgres-auth pg_isready -U postgres
   ```

3. **Неправильный JWT_SECRET**
   ```bash
   # Убедитесь, что JWT_SECRET одинаковый в Profile и Authorization
   # Проверьте .env файлы обоих сервисов
   ```

---

### Данные не попадают в ClickHouse

**Симптомы**: Пустая таблица `crypto.market_tickers` в Tabix

**Возможные причины и решения**:

1. **Kafka-ClickHouse Service не запущен**
   ```bash
   docker-compose ps kafka-clickhouse
   docker-compose logs -f kafka-clickhouse
   ```

2. **Kafka топик пуст**
   ```bash
   # Проверьте через Kafka UI (http://localhost:8084)
   # Топик: binance.miniticker
   ```

3. **ClickHouse недоступен**
   ```bash
   # Проверьте соединение
   docker-compose exec clickhouse clickhouse-client --query "SELECT 1"
   ```

---

### Высокое использование памяти

**Симптомы**: Docker контейнеры потребляют много памяти

**Решения**:

1. **Ограничьте память для Kafka**
   ```yaml
   # В compose.yaml
   environment:
     KAFKA_HEAP_OPTS: "-Xmx512M -Xms512M"
   ```

2. **Ограничьте память для Redis**
   ```yaml
   # В compose.yaml
   command: >
     redis-server
     --maxmemory 256mb
     --maxmemory-policy allkeys-lru
   ```

3. **Настройте Docker ресурсы**
   ```bash
   # Docker Desktop: Settings → Resources
   # Увеличьте доступную память до 8GB+
   ```

---

**Made with ❤️ using Go and microservices architecture**